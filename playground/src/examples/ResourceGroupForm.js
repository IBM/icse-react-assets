import { ResourceGroupForm } from "icse-react-assets";

export const ResourceGroupFormExample = () => {
  return (
    <ResourceGroupForm
      data={{
        name: "example-form",
      }}
      isModal={true}
    />
  );
};
