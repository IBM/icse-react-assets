const { toggleMarginBottom, addClassName } = require("./form-utils");

const {
  formatInputPlaceholder,
  hasInvalidName,
  validName,
} = require("./text-utils");
module.exports = {
  toggleMarginBottom,
  addClassName,
  formatInputPlaceholder,
  hasInvalidName,
  validName,
};
